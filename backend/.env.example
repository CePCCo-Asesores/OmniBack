# === Core ===
NODE_ENV=development
PORT=8080
SERVICE_NAME=omni-back

# === URLs públicas ===
PUBLIC_URL=http://localhost:3000
BACKEND_URL=http://localhost:8080

# === CORS ===
# Coma-separado. EJ: http://localhost:3000,https://app.tu-dominio.com
ALLOWED_ORIGIN=http://localhost:3000

# === Sesión / Auth ===
SESSION_SECRET=change-this-in-prod

# === Google OAuth ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Debe ser absoluto y apuntar a tu endpoint backend:
# EJ: https://api.tu-dominio.com/auth/google/callback
GOOGLE_CALLBACK_URL=http://localhost:8080/auth/google/callback

# === Build/Versionado (inyectados en CI) ===
APP_VERSION=1.0.0
GIT_SHA=local
BUILD_TIME=local
